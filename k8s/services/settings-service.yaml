apiVersion: apps/v1
kind: Deployment
metadata:
  name: settings-service
  namespace: food-delivery-system
  labels:
    app: settings-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: settings-service
  template:
    metadata:
      labels:
        app: settings-service
    spec:
      containers:
        - name: settings-service
          image: food-delivery-settings-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50058
          envFrom:
            - configMapRef:
                name: food-delivery-config
            - secretRef:
                name: food-delivery-secrets
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: settings-service
  namespace: food-delivery-system
spec:
  selector:
    app: settings-service
  ports:
    - port: 50058
      targetPort: 50058
